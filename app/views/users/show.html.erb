<h1>
  <%= image_tag avatar_url(@user) %> 
  <%= @user.email %>
</h1>

<h2>Tracked shows</h2>
<ul>
  <% @user.shows.each do |show| %>
  <li>
    <%= link_to show.name, show_path(show.id) %>
  </li>
  <% end %>
</ul>